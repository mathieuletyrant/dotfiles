# Security and private files
.ssh/
.gnupg/
.aws/
.kube/
*.pem
*.key
*.gpg
.gitconfig.local
.netrc

# API tokens and secrets
.env
.env.*
*.token
.npmrc
.yarnrc
.boto
.composer/auth.json

# IDE and editor files
.idea/
.vscode/

# macOS
.DS_Store

# Config folder
config/.config/raycast/
config/.config/raycast-x/
config/.config/github-copilot
config/.config/op
config/.config/op/c
config/.config/configstore
config/.config/stripe
config/.config/spicetify

# App state / cache / locks
# ~/.config is symlinked into this repo, so apps dump runtime state here.
# Ignore the noise; track only intentional config below.
# arbor: daemon auth_token, do not commit
config/.config/arbor/
# gh: machine identity / auth state
config/.config/gh/hosts.yml
config/.config/entire/
config/.config/uv/
config/.config/nx/
config/.config/nxcloud/
# fish: generated by uv
config/.config/fish/
config/.config/supaterm/
config/.config/btop/
config/.config/ccstatusline/
config/.config/htop/
*.lock
